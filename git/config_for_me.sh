#!/bin/bash

git config --global push.default simple
git config --global user.name "dixx"
git config --global user.email "jenzdixx@googlemail.com"
git config --global core.excludesfile ~/.gitignore_global
